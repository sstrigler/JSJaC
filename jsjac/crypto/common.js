dojo.provide("jsjac.crypto.common");

/*
 * Configurable variables. You may need to tweak these to be compatible with
 * the server-side, but the defaults work in most cases.
 */
jsjac.crypto.hexcase = 0;  /* hex output format. 0 - lowercase; 1 - uppercase        */
jsjac.crypto.b64pad  = "="; /* base-64 pad character. "=" for strict RFC compliance   */
jsjac.crypto.chrsz   = 8;  /* bits per input character. 8 - ASCII; 16 - Unicode      */
